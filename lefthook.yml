pre-commit:
  parallel: false
  commands:
    whitespace:
      run: git diff --cached --check

commit-msg:
  commands:
    conventional-commit:
      run: bash tools/scripts/validate-commit-msg.sh {1}
